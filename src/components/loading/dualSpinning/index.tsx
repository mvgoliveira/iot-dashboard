import React, { ReactElement } from "react";

import { Container, StyledDualSpinning } from "./styles";

const DualSpinning = (): ReactElement => (
    <Container>
        <StyledDualSpinning />
    </Container>
);

export { DualSpinning };
